# Frozen module flags (contracts/p0-foundation/README.md). Upper-case exact values.
# Rollback = set the flag back to LEGACY + run tools/cdc-sync reverse for that group.
HRMS_FLAG_AUTH=NEW
HRMS_FLAG_EMPLOYEE=LEGACY
HRMS_FLAG_PAYROLL=LEGACY
HRMS_FLAG_PAYROLL_ENGINE=LEGACY
HRMS_FLAG_LEAVE=LEGACY
HRMS_FLAG_PERFORMANCE=LEGACY
HRMS_FLAG_REPORTING=LEGACY
