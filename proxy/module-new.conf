# NEW / NEW_READONLY(read): forward to Spring Boot (API) or React (UI) with the bearer token untouched.
if ($uri ~ ^/api/) { proxy_pass http://spring; }
proxy_pass http://react;
include /etc/nginx/hrms-headers.conf;
