[backend] F1 seed fixture tools/fixtures/pg/02_employee_data.sql inserts salary_records with explicit salary_id 1..43 but never advances seq_salary (V1 `start with 1`); on a stack brought up per backend/README.md the first POST /api/employees (SalaryService.createInitial -> SalaryRecordRepository.insert nextval('seq_salary')) fails with DuplicateKeyException pk_salary_records -> 500 INTERNAL_ERROR (traceId 5497db0524299f9b0e59e0ab895b1e99). SalaryRepositoryTest hides this with a per-test setval. Fix: setval in the fixture (or a V-migration). Harness applied `select setval('seq_salary',(select max(salary_id) from salary_records))` once to continue.
[backend] F2 Level 2 TARGET-DIFF employee.transfer.ok-same-dept-writes-history (tools/parallel-run EmployeeScenarios.java): setup creates the employee with the default hireDate 2025-06-02 then transfers with effectiveDate 2025-06-01; recorded expectation `[0].changeType=TRANSFER` contradicts the contract ordering `ORDER BY effective_date DESC, hist_id DESC` (openapi.yaml:637, EmployeeHistoryRepository) so the target correctly returns HIRE first. Both rows are written. Fix the fixture (transfer effectiveDate >= hireDate, or assert on the TRANSFER row by change_type) — do not change the API ordering.
[backend] F3 Level 3 VW_ORG_HIERARCHY mismatch after scripted writes: tests/reconciliation/pg/vw_org_hierarchy.sql applies `employment_status='ACTIVE'` inside the recursion (and at the root), whereas Oracle applies the view WHERE after CONNECT BY, so active reports of a terminated manager stay in the legacy view (ORG_LEVEL counts the terminated node, ORG_PATH contains its name). After terminating emp 21 (e2e) and emp 10005, emps 22/23/24/10006 vanish from the PG view but remain under Oracle semantics (see vw_org_hierarchy-oracle-vs-pg-semantics.txt). Same divergence hides the IS_LEAF case (Oracle CONNECT_BY_ISLEAF counts terminated children). Fix the PG query (walk all rows, filter status in the outer select) and add a golden fixture with a terminated mid-level manager — the pristine seed has none, which is why views-baseline.csv reconciles clean.
[backend] F4 Level 3 name normalisation: PKG_EMPLOYEE.create_employee/update_employee store UPPER(TRIM(first_name/last_name)); EmployeeService keeps the request casing, so employees created via the UI (e.g. 10005 "Grace Hopper") produce FULL_NAME/EMP_NAME/ORG_PATH/MANAGER_NAME cells that differ from what the legacy views would show ("GRACE HOPPER"). Not declared as a divergence in contracts/p3-employee or COMPONENT_MAPPING.md; the Level 2 registry masks it by sending upper-case names (missing fixture). Either normalise in EmployeeService (or the validation layer) or declare the divergence and adjust the reconciliation normalisation.
